** Both
== /etc/hosts
127.0.0.1   localhost
192.168.56.101  pg-server-01
192.168.56.102  pg-server-02
==


** Primary
== IMP Parameters -- need pg_ctl reload or restart
listen_addresses = '*'
wal_level = replica
max_wal_senders = 10
max_replication_slots = 10
hot_standby = on
wal_keep_size = 1GB
archive_mode = on
archive_command = 'cp %p /pgarchive/%f'
wal_log_hints = ON
==


== add entry $PGDATA/pg_hba.conf -- need pg_ctl reload or restart
host    replication     repluser        192.168.56.102/32       scram-sha-256
==

== Create user
CREATE ROLE repluser WITH REPLICATION LOGIN PASSWORD 'StrongPass';
==


** standby

== Empty $PGDATA
rm -rf /var/lib/pgsql/17/data/*
==


== clone
pg_basebackup -h 192.168.56.101 -U repluser -D /var/lib/pgsql/17/data -P -R -C -S standby1


export PGPASSWORD='repluser_password'
nohup pg_basebackup -h 192.168.56.101 -U repluser -D /var/lib/pgsql/17/data -P -R -C -S standby1 > /tmp/pg_basebackup.log 2>&1 &
==


== Check $PGDATA/postgresql.conf and postgresql.auto.conf -- -- need pg_ctl reload or restart if  any change
hot_standby=on

primary_conninfo = 
primary_slot_name='standby1'
==




*** Chekup


--> primary
SELECT * FROM pg_replication_slots;

SELECT * FROM pg_create_physical_replication_slot('standby1');
--SELECT * FROM pg_create_logical_replication_slot('slot1','pgoutput');
SELECT pg_drop_replication_slot('standby1');



SELECT * FROM pg_stat_replication;

SELECT application_name,
       client_addr,
       state,
       sync_state,
       pg_wal_lsn_diff(pg_current_wal_lsn(), replay_lsn) AS lag_bytes
FROM pg_stat_replication;


--> standby
SELECT pg_is_in_recovery();

SELECT * FROM pg_stat_wal_receiver;



*** Failover

--> standby
pg_ctl -D /var/lib/pgsql/17/data promote

SELECT pg_promote();






*** Rewind -- not worked as expected !!!!

--> primary 
wal_log_hints = ON

--> standby
GRANT pg_read_server_files TO repluser;

export PGPASSWORD='123'
pg_rewind -D /var/lib/pgsql/17/data --source-server="host=192.168.56.102 user=repluser dbname=postgres"


--  
export PGPASSWORD='123'
pg_rewind -D /var/lib/pgsql/17/data --source-server="host=192.168.56.101 user=postgres dbname=postgres"

touch $PGDATA/standby.signal

== add $PGDATA/postgresql.conf
hot_standby=on

== add connection postgresql.auto.conf
# Do not edit this file manually!
# It will be overwritten by the ALTER SYSTEM command.
primary_conninfo = 'user=repluser password=123 channel_binding=prefer host=192.168.56.101 port=5432 sslmode=prefer sslnegotiation=postgres sslcompression=0 sslcertmode=allow sslsni=1 ssl_min_protocol_version=TLSv1.2 gssencmode=prefer krbsrvname=postgres gssdelegation=0 target_session_attrs=any load_balance_hosts=disable'
primary_slot_name = 'standby1'


